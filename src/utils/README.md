- Run `node generateFonts.js` to generate `_Fonts.scss` 

&nbsp;

```

my-project/ 
├── public/
│   └── Font/
│       ├── my-font-1.ttf
│       └── my-font-2.otf
├── src/
│   ├── utils/
│   │   └── generateFonts.js
│   └── styles/
│       └── _Fonts.scss (will be generated)
└── package.json

```