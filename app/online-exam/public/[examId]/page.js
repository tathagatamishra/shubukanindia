import PublicExamPage from "@/components/Exam/Public/PublicExamPage";

export default function Page() {
  // The client component (PublicExamPage) uses useParams inside, so no props needed here.
  return <PublicExamPage />;
}
